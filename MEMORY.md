# MEMORY — Agentic AML & Regulatory Compliance Guardian (PoC)

Estado de execução das tarefas (`TASKS.md` §6), aprendizados, bugs persistentes e decisões tomadas no caminho (`SOUL.md`).

## Progresso

| Tarefa | Estado | Evidência |
| :--- | :--- | :--- |
| T0.1 — Projeto Python e guarda da stack | Concluída (`748d312`) | `pip install -e .[dev]` exit 0 · `pip check` ok · `.venv` `pytest -k stack`: 27 passed · `spacy.load('pt_core_news_lg')` ok |
| T0.2 — Contratos de ingestão e estado (DT-01 a DT-05) | Concluída | `pytest -k dt_ingestao`: 80 passed · Revisor Python: 1 médio resolvido, 2 baixos adiados |
| T0.3 — Contratos de triagem, investigação, normas e dossiê (DT-06 a DT-11, DT-16) | Concluída | `pytest -k dt_investigacao`: 118 passed · `pytest tests/contracts`: 198 passed · Revisor Python: 1 médio resolvido (`cc4001_incisos` no caminho da triagem), 0 alto |
| T0.4 — Auditoria, envelope A2A, aprovação e estado do grafo (DT-12 a DT-15, `InvestigationState`) | Concluída | `pytest -k dt_envelope`: 132 passed · `pytest tests/contracts`: 330 passed (DT-01 a DT-16) · Revisor Python: 1 crítico resolvido (estado aceitava artefato de fase futura), 1 alto resolvido (`prazo_comunicacao` no mesmo dia da decisão) |
| T0.6 — Mapeamento de tipologias e loader do SAML-D | Concluída | `pytest -k saml_loader`: 27 passed · suíte: 463 passed · ruff ok · loader sobre o SAML-D real: 9.504.852 linhas, 28 rótulos, 0 divergências · Revisor Python: AJUSTAR com 3 bloqueadores, todos resolvidos (1 alto: inciso inexistente na norma; 2 médios: cabeçalho validado duas vezes, teste de flag ambígua) |
| T0.7 — Camada brasileira e `core_sintetico.sqlite` | Concluída | `pytest -k camada_br`: 54 passed · suíte: 517 passed · ruff ok · banco gerado do SAML-D real: 9.504.852 linhas lidas → 971.289 transações, 12.950 contas/clientes, 9.873 suspeitas (100% das do dataset), 28 rótulos, 221,7 MB em 183 s · SHA-256 do conteúdo `00a6d2f1…5f14ca71` reproduzido em 3 gerações independentes · Revisor Python: APROVAR sem bloqueadores, 1 médio resolvido (injeção de `conta_bancaria` virou invariante imposta), 2 baixos (um fechado pela mesma correção, outro já coberto pelos testes de determinismo) |
| T0.5 — Configuração versionada | Concluída | `pytest -k config`: 79 passed · `pytest -k stack`: 27 passed · ruff ok · Revisor Python: APROVAR, 3 médios resolvidos (`aprovacao.em` aceitava data como string; `critico` aceitava `1`/`1.0`; guarda de provedor com termos faltantes) |
| T0.8 — Gerador de alertas legado simulado (DT-01) | Concluída | `pytest -k gerador_alertas`: 9 passed · suíte: 526 passed · ruff ok · gerado sobre o `core_sintetico` real: 42.440 alertas em 102,2 s, taxa de falso positivo geral **93,85%** (39.831/42.440) — `LEG-VALOR-ELEVADO-01` 94,81% (32.368 alertas), `LEG-CONTAGEM-VELOC-01` 90,77% (10.072 alertas), dentro da faixa 90-95% do `SPEC.md` §8.3 sem recalibrar limiares provisórios · Revisor Python: 1 crítico resolvido (`clientes[conta]` sem FK entre `transacoes.sender_account` e `contas.conta` podia levantar `KeyError`; conta sem cliente correspondente agora é ignorada), 2 altos avaliados como inalcançáveis por construção (`incluidas` em `_episodios` nunca vazia — documentado em comentário, não em guarda de runtime), 1 médio adiado (rescan O(n·k) em `_episodios`, sem impacto medido nos 102,2 s reais) |

## Decisões no caminho

- **2026-09-15 · T0.1 · Ask First aprovado:** dependências da stack do `SPEC.md` §6 com limites inferiores; complementares `uvicorn` e `pyyaml`.
- **2026-09-15 · T0.1:** `semgrep` fica no extra `security` (sem suporte nativo estável no Windows; rodar via WSL/Docker no M6), fora de `dev`.
- **2026-09-15 · T0.1:** `.venv` com Python 3.13 (3.14 é o padrão do sistema, mas há risco de wheels ausentes para spaCy/Presidio/ChromaDB).
- **2026-09-15 · T0.1:** `pt_core_news_lg` 3.8.0 declarado por URL direta no `pyproject.toml` (casado com `spacy>=3.8,<3.9`).
- **2026-09-15 · T0.2:** `amount_brl` > 0 com no máximo 2 casas decimais; `float`/`bool` recusados (evita imprecisão binária). Sem `max_digits` (SPEC não define teto).
- **2026-09-15 · T0.2:** DT-04 aceita em conta/nome/`cpf_cnpj` só token `^[A-Z]+_\d{2,}$`; amarrar prefixo por campo (`CPF_`, `CONTA_`, `NOME_`) fica para a T1.x do sanitizador, que define os tipos.
- **2026-09-15 · T0.2:** `PROPOR_ARQUIVAMENTO` é `TriageLevel`, não estado; DT-05 valida coerência estado ↔ nível pelo RF-10 (sem nível antes de `TRIAGED`; `INVESTIGAR` em `INVESTIGATING/RESEARCHING/REVIEWING`; `NEEDS_HUMAN` livre e exige `failure_reason`). Prazos do DT-05 anuláveis (calculados no nó Dossiê + Prazo).
- **2026-09-15 · T0.3:** enum `Typology` usa os rótulos literais do SAML-D (`"Deposit-Send"`, …) + `NENHUMA`, idênticos ao enum de geração do `AGENTS.md` §4.2; o loader da T0.6 compara com esses valores. `feature_id` no formato `^F\d{2,}$` (`3 → "F03"`).
- **2026-09-15 · T0.3:** versões `rules`/`mapping`/`features` são int estrito ≥ 1 (YAML da T0.5/T0.6, `features_version = 1`); `corpus`/`prompt`/`model` são string. No DT-11 as 6 chaves são obrigatórias; no caminho `PROPOR_ARQUIVAMENTO` (só DT-06) só `rules` é não nula, e o dossiê não tem tipologia, evidência, citação nem `ai_generated_fields`. Caminho investigado exige os 4 `ai_generated_fields`, tipologia e todas as versões.
- **2026-09-15 · T0.3:** DT-11 sem campo novo: `evidence[]` reutiliza o item do DT-16 (≤ 3, com `transaction_ids`); `citations[]` é DT-09; `cc4001_incisos[]` só romano (`^[IVXLC]+$`); `dossier_id` UUID; `status` ∈ {`DRAFT_READY`, `SUBMITTED`, `APPROVED`, `RETURNED`}; `COS`↔`COMUNICAR`, `ARQUIVAMENTO`↔`ARQUIVAR` (`INCONCLUSIVO` recusado, RF-08); `COS` exige ≥ 1 citação (RF-07). `deadlines` só valida ordem; o cálculo exato fica no RF-09.
- **2026-09-15 · T0.3:** DT-06 recusa `PROPOR_ARQUIVAMENTO` com detector crítico (`AGENTS.md` §2.2). DT-10 recusa `chunk_id` verificado e rejeitado ao mesmo tempo; `grounding_raw_ratio` não é recalculado (o RF-06 conta também `chunk_id` fora do recuperado). `missing_evidence_ids(DT-07, DT-16)` devolve as evidências inexistentes em vez de levantar, porque o RF-05 manda descartar e registrar. Texto normativo (`text`, `quoted_text`, `applicability`) não é normalizado no contrato.
- **2026-09-15 · T0.4:** DT-14 fica em `contracts/envelope.py` (limite de 300 linhas) e é reexportado por `contracts/runtime.py`. `schema_version` por aresta do `AGENTS.md` §5: `DT-04.v1`, `DT-06.v1`, `DT-04+DT-06.v1`, `DT-16+DT-07.v1`, `retrieved_chunk_ids.v1`, `DT-09[].v1`, `DT-10.v1`, `error.v1`. Payloads compostos usam os nomes de campo do `InvestigationState` (`sanitized_alert`, `triage`, `features`, `investigation`, `retrieved_chunk_ids`, `citations`). O envelope recusa aresta fora da tabela, `type`/`schema_version` incoerentes com a aresta e `alert_id` do payload diferente do envelope; condições de roteamento ("ao menos 1 citação verificada") ficam no grafo (T1.10). `SCHEMA_REGISTRY` e `EDGES` são `MappingProxyType` (imutáveis).
- **2026-09-15 · T0.4:** DT-12 exige a chave de todos os campos do RF-11 (nuláveis os que não se aplicam); `actor` é papel (`sistema`/`analista`/`compliance_officer`), nunca texto livre; `event_type` só `^[A-Z][A-Z0-9_]*$`; `prev_hash` é `null` se e somente se `seq = 1`; `model_id`/`prompt_sha256`/`prompt_version` vêm juntos; tokens > 0 exigem `model_id`. `event_key` só precisa começar pelo `alert_id` (separador e formato de `node + attempt` ficam para a T1.1).
- **2026-09-15 · T0.4:** DT-13 não normaliza `text` (preserva offsets); entidades `[start, end)` dentro do texto e sem sobreposição; `type` `^[A-Z]+$`. DT-15: `justification` com `strip` antes de contar ≥ 50; `decided_by_role` só `compliance_officer`; `prazo_comunicacao` é `date` e existe se e somente se `COMUNICAR`, estritamente após a data de `decided_at` em Brasília e em dia de semana. Brasília = offset fixo UTC−3 (`BRASILIA`, sem horário de verão desde 2019; evita `tzdata` no Windows); feriados e o cálculo exato ficam no RF-09.
- **2026-09-15 · T0.4 · interpretação do SPEC §9.3:** `triage` e `features` são anuláveis no `InvestigationState` (o grafo nasce em `SANITIZED`, e o caminho `PROPOR_ARQUIVAMENTO` não tem DT-16). Coerência RF-10: `RECEIVED` recusado; teto por fase (`_FIRST_STAGE`: `triage` ≥ `TRIAGED`, `features`/`investigation` ≥ `INVESTIGATING`, `retrieved_chunk_ids`/`citations` ≥ `RESEARCHING`, `review` ≥ `REVIEWING`, `dossier` ≥ `DRAFT_READY`); após triagem exige `triage`; `INVESTIGATING/RESEARCHING/REVIEWING` exigem `INVESTIGAR`; `RESEARCHING/REVIEWING` exigem `investigation`; `dossier` existe se e somente se o estado é de dossiê, com `dossier.status = state`; `investigation` exige DT-16 com todas as evidências; `NEEDS_HUMAN` só exige `failure_reason` e preserva o acumulado. `attempts` por `Node` com valor ≥ 1; `budget` não limita 2.000 tokens (excesso leva a `NEEDS_HUMAN`).
- **2026-09-15 · T0.1:** pytest deseleciona `ollama` e `network` por padrão via `addopts`; `-m ollama`/`-m network` na linha de comando sobrepõe.
- **2026-09-15 · T0.5 · Ask First aprovado:** `rules_version: 1` provisória — fragmentação ≥ 3 transações < R$ 10.000,00 em 7 dias; camadas com profundidade ≥ 2, ≥ 3 contrapartes, 30 dias; espécie → exterior em 7 dias; listas `pep`/`ceis`/`cnep`. `selection.min_score` 0,50 (cosseno do trecho antes do RRF, provisório). Baseline `tempo_manual_min` 120 / `tempo_revisao_min` 15 (premissas do autor, provisórias). `cache.threshold` 0,92 e parâmetros do ADR-013 como definitivos.
- **2026-09-15 · T0.5 · Ask First aprovado:** `config/feriados.yaml` = dias não úteis do mercado financeiro pela Res. CMN 4.880/2020, art. 6º (segunda e terça de Carnaval, Corpus Christi) + feriados de lei federal (Lei 662/1949 com redação da Lei 10.607/2002; Lei 6.802/1980; Lei 14.759/2023 a partir de 2024), 2022–2027, 70 datas. Sexta-feira da Paixão fora: não consta do art. 6º e é feriado religioso municipal (Lei 9.093/1995, art. 2º). Textos conferidos no Planalto e na API de normativos do BCB. O intervalo 2022–2023 presume o período do SAML-D: confirmar na T0.6. `is_feriado` fora do período levanta `ValueError` (nunca vira dia útil).
- **2026-09-15 · T0.5 · interpretação:** "`litellm.yaml` sem nome de provedor proprietário em código" = (a) nenhum literal de provedor/modelo em `src/` (varredura AST, sem docstrings) e (b) a PoC resolve só `ollama`/`ollama_chat` em `localhost`; a execução do RNF-10 com segundo provedor (M7) revisa (b). `model_list` no formato do LiteLLM Router, alias `investigacao` → `ollama_chat/qwen2.5:1.5b`. O loader impõe `temperature` 0 (RNF-09) e `num_predict` ≤ 60 (RF-05); detectores críticos obrigatórios com `critico: true`; `limiar_brl` reutiliza `AmountBRL` (string decimal). Aceite gravado por seção em `aprovacao{por, em, situacao, referencia}`. Testes divididos em `test_config.py` e `test_config_feriados.py` (limite de 300 linhas).

- **2026-09-16 · T0.6 · Ask First aprovado:** `data/mappings/saml_d_to_cc4001.yaml` é chaveado pelo rótulo BRUTO do SAML-D (`Layered_Fan_In`, `Single_large`, `Stacked Bipartite` com espaço), com campo `typology` na grafia do enum. A diferença de grafia é dado versionado, nunca conversão em código — nenhuma regra geral leva `Single_large` a `Single Large Transaction`. Os 11 rótulos `Normal_*` ficam em bloco `normais:` separado, sem enquadramento: rótulo normal não ganha inciso por omissão. `alineas` por tipologia preserva o que a tabela do SPEC §3.2 cita (Structuring d/e/k; Smurfing d/e/f/l/m) e não cabe no tipo `Inciso`.
- **2026-09-16 · T0.6 · fato conferido no SAML-D real:** 9.504.852 linhas, 28 rótulos (17 suspeitos + 11 normais, incluindo `Normal_Foward`, typo do dataset de origem, preservado como está). `Is_laundering` é perfeitamente coerente com o rótulo (9.873 suspeitas ⇔ flag 1), então o loader trata incoerência flag×rótulo como erro, não como aviso. O arquivo baixado usa a primeira alternativa do SPEC §8.3 (`Is_laundering`, `Laundering_type`); o loader aceita as duas grafias e recusa cabeçalho que traga ambas.

- **2026-09-16 · T0.7 · Ask First aprovado:** `Payment_type` → DT-02 com **ACH→PIX** e **Cheque→TED** (os outros 5 são diretos; os 7 rótulos cobrem os 6 valores do enum, sem sobra). Localização: **`UK`→`BR`** (96,6% das origens) e os outros 17 países em ISO-2, preservando ~96%/4% doméstico/exterior de que depende o detector crítico "espécie seguida de envio ao exterior". Câmbio: taxas fixas sintéticas em `config/cambio.yaml` (`cambio_version` 1), offline e determinístico — PTAX real recusado por exigir rede e não agregar realismo a base já sintética. Amostra do `core_sintetico`: por **conta remetente** (todas as linhas da conta entram juntas), com 100% das contas que têm transação suspeita mais 8.000 contas normais sorteadas pela seed — amostra por linha destruiria os padrões de janela móvel de que a T0.8 depende.
- **2026-09-16 · T0.7 · fato conferido no SAML-D real:** 7 `Payment_type`, 13 moedas, 18 países, 292.715 contas remetentes, 652.266 recebedoras; 4.950 contas remetentes têm transação suspeita e cobrem 732.524 linhas. Período do dataset: **2022-10-07 a 2023-08-23** — confirma a premissa 2022–2023 que a T0.5 deixou em aberto para o `config/feriados.yaml` (o intervalo 2022–2027 do arquivo cobre a base inteira). A menor conversão possível (Naira 13,91 × 0,0065 = R$ 0,09) não zera, mas a conversão recusa explicitamente resultado ≤ 0, porque o DT-02 exige `amount_brl` > 0.
- **2026-09-16 · T0.7 · interpretação:** sem ADR novo. O `TASKS.md` reserva o **ADR-014 ao formato do golden set (T0.9)** — o `.gitignore` já cita esse número — e o Ask First da T0.7 não pede ADR; as quatro decisões ficam versionadas nos blocos `aprovacao` de `config/cambio.yaml` e `data/mappings/saml_d_to_br.yaml`, como nas T0.5/T0.6. `transaction_id` é a posição da linha no CSV (`tx-00000001`), estável entre gerações e independente da amostra, para que a mesma transação tenha o mesmo id em qualquer seed. O rótulo de tipologia e `is_laundering` ficam gravados no banco para a estratificação da T0.9. `conteudo_sha256` exclui a tabela `meta` de propósito: proveniência (seed e versões) não é conteúdo, e incluí-la faria o hash mudar sem os dados mudarem.
- **2026-09-16 · T0.8 · Ask First aprovado:** `config/alert_rules.yaml` v1, duas regras clássicas de monitoramento legado — `valor_elevado`: transação isolada ≥ R$ 100.000,00 (agrupamento de 7 dias); `contagem_velocidade`: ≥ 6 transações da mesma conta remetente em janela de 30 dias, qualquer valor. Limiares calibrados sobre os percentis reais do `core_sintetico` (mediana ≈ R$ 31.444, p90 ≈ R$ 94.888, p95 ≈ R$ 127.862) e a distribuição de contagem por conta (12.950 contas, 1–754 transações), aprovados como provisórios; atingiram a faixa 90-95% do `SPEC.md` §8.3 de primeira, sem recalibração.
- **2026-09-16 · T0.8 · interpretação:** o `occurrence_window` de cada alerta é limitado ao **último gatilho de cada episódio contínuo** (gatilhos a ≤ janela da regra um do outro) — nunca cresce além da janela da própria regra (≤ 30 dias), o que garante a regra dos 45 dias (Circ. 3.978/2020, art. 39, parágrafo único) **por construção**, não por checagem em runtime. `is_laundering` (rótulo do SAML-D) nunca entra no contrato DT-01: só é lido fora dele, em `calcula_falso_positivo`, porque um monitoramento legado real não teria acesso ao rótulo verdadeiro. `alert_id` é `uuid5` determinístico (conta + regra + início + fim do episódio): mesmo `core_sintetico` sempre regera os mesmos alertas.

## Aprendizados

- O gerenciador de contexto de `sqlite3.Connection` faz commit mas **não fecha** a conexão: no Windows o arquivo continua bloqueado e a geração seguinte falha com `PermissionError` no `unlink`. Usar `contextlib.closing`. Quem pega isso é o teste que gera duas vezes no mesmo caminho (`test_banco_e_recriado_do_zero_e_nunca_completado`) — uma geração única passa e esconde o defeito até a T0.8 regerar o banco.
- Preenchimento com `zfill` só é injetor sobre números em forma canônica (`1` e `00000000001` colidem). As contas do SAML-D têm de 4 a 10 dígitos e **nenhuma** tem zero à esquerda (conferido nas 9.504.852 linhas, remetente e recebedor), então `conta_bancaria` recusa zero à esquerda: a injeção vira invariante imposta em vez de suposição sobre a base. Vale a regra geral — quando o revisor disser "impossível na prática", conferir na base antes de aceitar, porque é o que decide entre corrigir o código e só corrigir a docstring.
- `litellm` traz `openai` e `boto3` como dependências transitivas (instalados no `.venv`); a guarda (`tests/test_stack.py`) cobre apenas o declarado (incluindo `build-system.requires`) e os `import` em `src/`, conforme T0.1.
- `WebFetch` falha com `ECONNRESET` em planalto.gov.br; `curl` funciona. Normativos do BCB saem em JSON por `https://www.bcb.gov.br/api/conteudo/app/normativos/exibenormativo?p1=<Tipo>&p2=<número>` (`p1=Resolução CMN` para 4.880; `p1=Resolução` para normas antigas como 2.932).
- O art. 1º da CC 4.001/2020 tem **17 incisos (I a XVII)**, conferido no texto oficial do BCB — não presumido. O tipo `Inciso` (`^[IVXLC]+$`) só garante romano bem formado; a guarda de existência é `INCISOS_CC4001_ART1`, deliberadamente o conjunto da norma e não os 4 que a `mapping_version` 1 usa, para não travar versão futura. O Revisor Python da T0.6 afirmou que o art. 1º "menciona apenas {I, IV, IX, X}" e pediu essa trava: era alucinação de norma, corrigida contra o texto oficial.
- Revisor Python da T0.1: nomes exatos `azure`/`google-cloud`, `import_module(name=...)` e `build-system.requires` eram brechas da guarda; cobertos por casos de teste.
- `transacoes.sender_account` não tem FK para `contas.conta` no schema do `core_db.py` (T0.7): uma conta remetente sem cliente correspondente é dado real possível, não hipotético — o Revisor Python da T0.8 pegou o `clientes[conta]` sem tratamento (`KeyError`) que o dado sintético atual nunca dispara, mas o schema não impede. Ignorar essa conta (sem alerta, por falta de `sender_customer` para o DT-01) é o comportamento de um legado real sem cadastro; vale a mesma regra da T0.7 nº49 — invariante não garantida por schema precisa de tratamento explícito no código, não de suposição.
