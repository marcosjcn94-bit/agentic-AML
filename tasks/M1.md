# TASKS M1 — Esqueleto ponta a ponta

| Campo | Valor |
| :--- | :--- |
| Parte de | `TASKS.md` 1.0.0 (índice, formato das tarefas, layout e governança) |
| Status | Aprovado |
| Gate | Aprovado por: marcosjcn94@gmail.com — Data: 2026-09-15 |

Convenção, formato de tarefa, layout do repositório e governança estão no `TASKS.md` e não são repetidos aqui.

Objetivo (`PLAN.md` §3): um alerta sintético percorre o fluxo do `SPEC.md` §1 até `DRAFT_READY`, com todos os guardrails presentes.
Pré-condição: gate do M0 atendido.

## T1.1 — Persistência e auditoria encadeada

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-10 (base), RF-11 (base), DT-05, DT-12 |
| Depende de | Gate M0 |
| Entregas | `src/aml_guardian/persistence/`: `data/app.sqlite` em WAL, repositório de DT-05; `src/aml_guardian/audit/`: tabela de eventos com `seq`, `prev_hash`, `hash`, `event_key` único, triggers que abortam `UPDATE` e `DELETE`, função de recálculo da cadeia |
| Teste focado | `tests/audit/test_audit_chain.py` · `pytest -k audit_chain`: `UPDATE` e `DELETE` abortam; cadeia recalculada válida após N eventos; evento com mesmo `event_key` não duplica; evento sem dado pessoal nem prompt bruto (só `prompt_sha256`) |
| Ask First | — |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde |

## T1.2 — Sanitizador e Vault

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-02, DT-04, ADR-003, ADR-012 |
| Depende de | T1.1 |
| Entregas | `src/aml_guardian/sanitizer/`: regex com validação de DV para CPF e CNPJ (com e sem máscara), agência/conta, nomes via Presidio + spaCy `pt_core_news_lg`; tokens `<TIPO>_<NN>` por ordem de aparição; Vault AES-GCM em memória com `KeyProvider`; fail-closed; evento `ALERT_RECEIVED` e transição `RECEIVED → SANITIZED` com `sanitizer_version` |
| Teste focado | `tests/sanitizer/test_sanitizer.py` · `pytest -k sanitizer`: CPF/CNPJ gerados no teste com e sem máscara tokenizados; mesmo valor → mesmo token no mesmo alerta; 11 dígitos com DV inválido → `NEEDS_HUMAN` sem chamar LLM; exceção interna → `NEEDS_HUMAN`; Vault não escreve em disco; mesma entrada → mesmo mapa (reconstrução, ADR-012); teste local de canário sobre DT-04 e `app.sqlite` |
| Ask First | — |
| Revisores | Revisor Python; Guardião de privacidade e segurança (obrigatório) |
| Pronto quando | Filtro verde e veredito do Guardião sem achado alto |

## T1.3 — Servidores MCP de dados (MCP-01, MCP-02)

| Campo | Conteúdo |
| :--- | :--- |
| IDs | MCP-01, MCP-02, RF-02, `SPEC.md` §9.2 |
| Depende de | T1.2 |
| Entregas | `src/aml_guardian/mcp_servers/`: servidores stdio `get_customer_history` (`window_days` ≤ 180) e `check_restriction_lists` sobre `core_sintetico`; cliente comum com timeout 2 s, erro `{error_code, retryable}` e evento `TOOL_CALLED` com hash da entrada; saída de ambos passando pelo Sanitizador antes de chegar a um agente |
| Teste focado | `tests/mcp_servers/test_mcp_dados.py` · `pytest -k mcp_dados`: servidor lento → erro estruturado `retryable`; `window_days` 181 rejeitado; saída do MCP-01 sem conta ou documento em claro; MCP-02 devolve só `{pep, ceis, cnep, list_version}`; evento `TOOL_CALLED` gravado |
| Ask First | — |
| Revisores | Revisor Python; Guardião de privacidade e segurança |
| Pronto quando | Filtro verde e canário local sem ocorrência na saída das tools |

## T1.4 — Triagem inicial

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-03 (base), DT-06, MCP-02 |
| Depende de | T1.3 |
| Entregas | `src/aml_guardian/triage/`: os quatro detectores críticos do RF-03 em forma simples (fragmentação; dispersão/concentração em camadas com profundidade ≥ 2; depósito em espécie seguido de envio transfronteiriço; acerto em lista de restrição via MCP-02), executados antes de qualquer proposta de arquivamento; `TriageDecision` com `fired_rules[]{rule_id, description, critical}` e `rules_version`; MCP-02 indisponível → `NEEDS_HUMAN` |
| Teste focado | `tests/triage/test_triage.py` · `pytest -k triage`: cada detector crítico disparado → `INVESTIGAR`, nunca `PROPOR_ARQUIVAMENTO`; alerta sem detector → `PROPOR_ARQUIVAMENTO`; mesma entrada + mesma `rules_version` → mesma decisão; MCP-02 fora → `NEEDS_HUMAN` |
| Ask First | Limiares (herdados da T0.5); nova `rules_version` |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde, sem LLM no módulo |

## T1.5 — Pré-passo de indicadores (DT-16)

| Campo | Conteúdo |
| :--- | :--- |
| IDs | DT-16, MCP-01, MCP-02, `AGENTS.md` §4.3 |
| Depende de | T1.4 |
| Entregas | `src/aml_guardian/features/`: cálculo de F01–F13 e F14+ pelo catálogo `features_version = 1`, com nomes e formatos de `scripts/bench_ollama.py:128-146`; MCP-01 com `window_days = 180`; `transaction_ids[]` por feature |
| Teste focado | `tests/features/test_features.py` · `pytest -k features`: alerta sintético com resultado conhecido por feature; valores inteiros exceto F11 (1 casa decimal + `x`); F13 no formato `pep:sim/nao,ceis:sim/nao,cnep:sim/nao`; F14+ ordenadas por `brl` decrescente e só com tokens |
| Ask First | Qualquer indicador novo (`features_version`) |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde |

## T1.6 — Investigação v1

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-05, DT-07, DT-16, RNF-10, ADR-010, ADR-013, `AGENTS.md` §4 |
| Depende de | T1.5 |
| Entregas | `src/aml_guardian/investigation/contract.py` com instruções (`bench_ollama.py:58-62`), bloco do alerta (`:152-156`, sem nonce), schema de geração (`:164-177`), expansão (`:56`, `:190-191`) e validação (`:180-200`) — **sem alterar** `scripts/bench_ollama.py`; montador que conta tokens pelo tokenizador do modelo via LiteLLM e aplica o teto de 300 com remoção de F14+ de menor `brl` (empate: maior `feature_id` sai primeiro); Router LiteLLM pelo alias de `config/litellm.yaml`; callback que conta chamadas e tokens sem logar prompt nem resposta; pós-processamento RF-05; evento com `model_id`, `prompt_sha256`, `prompt_version`, `tokens_in`, `tokens_out`, `latency_ms` |
| Teste focado | `tests/investigation/test_investigation.py` · `pytest -k investigation`: paridade com o script (instruções e schema idênticos); prompt acima de 300 tokens remove F14+ na ordem definida; ainda acima → `NEEDS_HUMAN` sem chamada; JSON inválido após 2 retries → `NEEDS_HUMAN`; evidência inexistente descartada e registrada; detector crítico ou tipologia crítica com `ARQUIVAR` → `NEEDS_HUMAN`; modelo simulado por `mock_response` do LiteLLM. Teste real com marcador `ollama` |
| Ask First | Qualquer alteração de prompt, schema, parâmetros ou modelo |
| Revisores | Revisor Python; Guardião de privacidade e segurança (prompt) |
| Pronto quando | Filtro verde, `pytest -m ollama -k investigation` verde no hardware de referência e canário local sem ocorrência no prompt capturado |

## T1.7 — Corpus normativo base e MCP de normas (MCP-03, MCP-04)

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-14 (base), DT-08, MCP-03, MCP-04, ADR-009 |
| Depende de | T1.3 |
| Entregas | `src/aml_guardian/norms/`: download da Circ. BCB 3.978/2020 e da CC BCB 4.001/2020 pelas URLs do `SPEC.md` §3.1 e §3.2, com `source_url`, `downloaded_at`, `doc_sha256`; chunking por dispositivo com `article_ref` (ex.: `Circ3978/art43/p1`) e `text_sha256` do trecho normalizado (NFC, espaços colapsados, trim); `corpus_version` derivado dos `doc_sha256`; coleção ChromaDB `norms` com um candidato FastEmbed do `SPEC.md` §6 em caráter provisório; servidores stdio `search_norms` e `get_norm_passage` usando o cliente comum da T1.3 |
| Teste focado | `tests/norms/test_corpus.py` · `pytest -k corpus`: chunking de texto normativo de fixture produz `article_ref` esperados; normalização estável do hash; reingestão sem mudança mantém `corpus_version`; MCP-03 com `top_k` 9 rejeitado e sem texto no retorno; MCP-04 com `chunk_id` de outra versão → 404 lógico. Download real com marcador `network`, verificando que `Circ3978/art43/p1` existe |
| Ask First | Escolha definitiva do modelo de embedding (fica para o M4, novo ADR) |
| Revisores | Revisor Python; Guardião de privacidade e segurança (servidor MCP) |
| Pronto quando | Filtro verde e `pytest -m network -k corpus` verde |

## T1.8 — Recuperação, Seleção e Revisor

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-06 (base), RF-07 (base), DT-09, DT-10, ADR-013 A |
| Depende de | T1.7 |
| Entregas | `src/aml_guardian/retrieval/`: consulta por template a partir da tipologia e dos incisos mapeados, sem dado pessoal, `top_k = 8`, sem cache; Seleção com preferenciais do mapeamento, desempate RRF (vetorial × BM25), descarte abaixo de `selection.min_score`, ≤ 3 citações com `quoted_text` copiado via MCP-04 e `applicability` do mapeamento; `src/aml_guardian/review/`: verificação de `chunk_id` na `corpus_version` vigente e SHA-256 do trecho normalizado → DT-10; incremento de `mapping_version` com preferenciais e `applicability` curados para a tipologia do alerta E2E |
| Teste focado | `tests/review/test_review.py` · `pytest -k review`: citação com 1 caractere adulterado rejeitada; `chunk_id` fora dos recuperados descartado e contado como não verificado; `COS` sem citação verificada → `NEEDS_HUMAN`; `tests/retrieval/test_selection.py` · `pytest -k selection`: preferencial vence RRF; abaixo de `min_score` descartado; nunca mais de 3 |
| Ask First | Valor provisório de `selection.min_score`; texto curado de `applicability` e `article_ref` preferenciais da tipologia E2E (completados no M4) |
| Revisores | Revisor Python |
| Pronto quando | Filtros verdes, sem LLM nos módulos |

## T1.9 — Dossiê e prazos base

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-08 (base), RF-09 (base), DT-11 |
| Depende de | T1.8 |
| Entregas | `src/aml_guardian/dossier/`: templates Jinja2 `COS` e `ARQUIVAMENTO`, `ai_generated_fields = [typology_hypothesis, confidence, recommendation, evidence_feature_ids]` marcados "gerado por IA", transações das evidências anexadas pelo código, só citações verificadas; `src/aml_guardian/deadlines/`: `prazo_interno = selecao_em + 5 dias corridos` e `prazo_regulatorio_analise = selecao_em + 45 dias`, persistidos em DT-05 |
| Teste focado | `tests/dossier/test_dossier.py` · `pytest -k dossier`: snapshot de `COS` e de `ARQUIVAMENTO`; 100% válido no DT-11; `INCONCLUSIVO` → `NEEDS_HUMAN`; campo obrigatório ausente → `NEEDS_HUMAN`; `tests/deadlines/test_deadlines.py` · `pytest -k deadlines`: +5 e +45 com virada de mês |
| Ask First | — |
| Revisores | Revisor Python |
| Pronto quando | Filtros verdes |

## T1.10 — Grafo LangGraph

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-10 (base), RF-11 (base), DT-14, `SPEC.md` §9.3, `AGENTS.md` §1 e §5 |
| Depende de | T1.6, T1.9 |
| Entregas | `src/aml_guardian/graph/`: nós `triage`, `investigation`, `retrieval`, `selection`, `reviewer`, `dossier`, `human_handoff`; arestas e envelopes do `AGENTS.md` §5; checkpoint `langgraph-checkpoint-sqlite` após cada nó; evento DT-12 idempotente por nó (`event_key = alert_id + node + attempt`); `NEEDS_HUMAN` com `failure_reason` preservando o `InvestigationState` |
| Teste focado | `tests/graph/test_graph.py` · `pytest -k graph`: `INVESTIGAR` percorre investigation → retrieval → selection → reviewer → dossier; `PROPOR_ARQUIVAMENTO` vai direto ao dossier; envelope `ERROR` de qualquer nó → `NEEDS_HUMAN` preservando produção parcial; envelope com `payload` inválido rejeitado; checkpoint presente após cada nó |
| Ask First | Qualquer mudança em arestas, envelope ou estados |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde com nós reais e tools simuladas |

## T1.11 — API base (API-01, API-03, API-09)

| Campo | Conteúdo |
| :--- | :--- |
| IDs | API-01, API-03, API-09, RF-01 (base), ADR-007, `SPEC.md` §9.1 |
| Depende de | T1.10 |
| Entregas | `src/aml_guardian/api/`: `POST /alerts` (papel `sistema`, valida DT-01, registra `selecao_em`, sanitiza antes de persistir, dispara o grafo, `202`); `GET /alerts/{alert_id}` e `GET /dossiers/{alert_id}` mascarados; `GET /health` com `{status, ollama, chroma, sqlite, corpus_version}`; `Authorization: Bearer` por papel lido de variável de ambiente; corpo de erro `{error: {code, message, trace_id}}` sem dado pessoal |
| Teste focado | `tests/api/test_api.py` · `pytest -k api`: tokens por papel definidos com `monkeypatch.setenv` (nunca `.env`); `202`, `401`, `403`, `404`, `422`; mensagem de erro de payload inválido sem valor enviado; `/health` → `503` com dependência fora |
| Ask First | Qualquer mudança de rota ou payload |
| Revisores | Revisor Python; Guardião de privacidade e segurança (fronteira de entrada) |
| Pronto quando | Filtro verde |

## T1.12 — E2E e canário RNF-05 inicial

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-01 a RF-11, RF-14 (base), RNF-05, API-01, API-03 |
| Depende de | T1.11 |
| Entregas | `tests/e2e/test_e2e.py`; `tests/privacy/test_canary.py` com valores sintéticos marcados gerados em tempo de execução e procurados nos prompts capturados pelo callback do LiteLLM, nos logs e em `data/app.sqlite` |
| Teste focado | `pytest -k e2e`: alerta com nível `INVESTIGAR` via API-01 chega a `DRAFT_READY` com ≥ 1 citação verificada; alerta com nível `PROPOR_ARQUIVAMENTO` chega a `DRAFT_READY` com 0 chamadas de modelo contadas no callback; cadeia de auditoria recalculada válida ao final. `pytest -k canary`: 0 ocorrências nos três destinos |
| Ask First | — |
| Revisores | Revisor Python; Guardião de privacidade e segurança |
| Pronto quando | Filtros verdes com Ollama real (`-m ollama`) e com modelo simulado |

## T1.13 — Avaliador inicial

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RNF-03 (informativo), `SPEC.md` §10 |
| Depende de | T1.12 |
| Entregas | `scripts/evaluate.py` gerando `reports/eval-<data>.json` e `.md` com versões gravadas, parâmetros de `config/baseline.yaml`, latência do fluxo `INVESTIGAR` medida (informativa) e cada gate do `SPEC.md` §10 ainda não implementado marcado como "não medida" |
| Teste focado | `tests/scripts/test_evaluate.py` · `pytest -k evaluate`: relatório gerado sobre amostra sintética; gate não implementado aparece como "não medida", nunca como aprovado; relatório sem dado pessoal |
| Ask First | — |
| Revisores | Avaliador; Revisor Python |
| Pronto quando | Relatório do M1 gerado e anexado ao `MEMORY.md` com o overhead do grafo sobre os 15,0 s estimados no ADR-013 |

## Gate de saída do M1

| Item do `PLAN.md` §3 | Tarefa | Evidência |
| :--- | :--- | :--- |
| Alerta `INVESTIGAR` via API-01 chega a `DRAFT_READY` com ≥ 1 citação verificada | T1.12 | `pytest -k e2e` |
| Alerta `PROPOR_ARQUIVAMENTO` chega a `DRAFT_READY` com 0 chamadas de modelo | T1.12 | `pytest -k e2e` |
| Canário RNF-05 inicial (prompts, logs, `data/app.sqlite`) sem ocorrências | T1.12 | `pytest -k canary` |
| Cadeia de auditoria recalculada válida | T1.1, T1.12 | `pytest -k "audit_chain or e2e"` |
| Latência do fluxo `INVESTIGAR` registrada como informativa | T1.13 | `reports/eval-<data>.md` |
| Revisor Python, Guardião de privacidade e segurança, Avaliador | Todas | Achados altos resolvidos |

