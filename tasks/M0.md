# TASKS M0 — Fundação, contratos e dados sintéticos

| Campo | Valor |
| :--- | :--- |
| Parte de | `TASKS.md` 1.0.0 (índice, formato das tarefas, layout e governança) |
| Status | Aprovado |
| Gate | Aprovado por: marcosjcn94@gmail.com — Data: 2026-09-15 |

Convenção, formato de tarefa, layout do repositório e governança estão no `TASKS.md` e não são repetidos aqui.

Objetivo (`PLAN.md` §3): repositório executável, contratos tipados e dados sintéticos rastreáveis, antes de qualquer nó.

## T0.1 — Projeto Python e guarda da stack

| Campo | Conteúdo |
| :--- | :--- |
| IDs | `SPEC.md` §6 (obrigatória e proibida) |
| Depende de | — |
| Entregas | `pyproject.toml` (Python ≥ 3.11, dependências da stack obrigatória, extras de dev), configuração pytest (marcadores `ollama`, `network`) e ruff, esqueleto `src/aml_guardian/` e `tests/`, acréscimos ao `.gitignore` |
| Teste focado | `tests/test_stack.py` · `pytest -k stack`: nenhuma dependência declarada nem `import` em `src/` da lista proibida (`openai`, `anthropic`, `boto3`, `google-cloud-*`, `azure-*`); Python ≥ 3.11 |
| Ask First | **Pedido único em bloco** com todas as dependências da stack do `SPEC.md` §6, incluindo o modelo spaCy `pt_core_news_lg` |
| Revisores | Revisor Python |
| Pronto quando | `pip install -e .[dev]` conclui e `pytest -k stack` passa |

## T0.2 — Contratos de ingestão e estado (DT-01 a DT-05)

| Campo | Conteúdo |
| :--- | :--- |
| IDs | DT-01, DT-02, DT-03, DT-04, DT-05 |
| Depende de | T0.1 |
| Entregas | `src/aml_guardian/contracts/ingestion.py`: modelos Pydantic v2 com os campos do `SPEC.md` §8.2; `alert_id` UUID; `amount_brl` decimal; enum `payment_type` (`PIX`, `TED`, `ESPECIE_DEPOSITO`, `ESPECIE_SAQUE`, `CARTAO`, `TRANSFERENCIA_INTERNACIONAL`); enum de estados do RF-10 |
| Teste focado | `tests/contracts/test_dt_ingestao.py` · `pytest -k dt_ingestao`: payload válido do exemplo API-01 do `SPEC.md` §9.1; campo obrigatório ausente, `payment_type` desconhecido e `amount_brl` não decimal rejeitados; DT-04 sem campo de dado pessoal em claro |
| Ask First | Qualquer campo além dos listados no `SPEC.md` §8.2 |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde e cada modelo cita o seu DT na docstring |

## T0.3 — Contratos de triagem, investigação, normas e dossiê (DT-06 a DT-11, DT-16)

| Campo | Conteúdo |
| :--- | :--- |
| IDs | DT-06, DT-07, DT-08, DT-09, DT-10, DT-11, DT-16 |
| Depende de | T0.2 |
| Entregas | `src/aml_guardian/contracts/pipeline.py`: DT-07 com enum das 17 tipologias suspeitas + `NENHUMA`, `confidence` ∈ [0, 1], `recommendation` ∈ {`COMUNICAR`, `ARQUIVAR`, `INCONCLUSIVO`}, `evidence_feature_ids` ≤ 3; DT-09 `applicability` ≤ 300 caracteres; DT-11 com `type` ∈ {`COS`, `ARQUIVAMENTO`}, `deadlines` e `versions{rules, corpus, mapping, features, prompt, model}`; DT-16 com `features[]{feature_id, name, value, transaction_ids[]}` e `features_version` |
| Teste focado | `tests/contracts/test_dt_investigacao.py` · `pytest -k dt_investigacao`: tipologia fora do enum, `confidence` 1,01, 4 evidências e `applicability` com 301 caracteres rejeitados; validação cruzada de evidência ∈ DT-16 exposta como função testável |
| Ask First | Qualquer campo além dos listados no `SPEC.md` §8.2 |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde |

## T0.4 — Auditoria, envelope A2A, aprovação e estado do grafo (DT-12 a DT-15, `InvestigationState`)

| Campo | Conteúdo |
| :--- | :--- |
| IDs | DT-12, DT-13, DT-14, DT-15, `SPEC.md` §9.3 |
| Depende de | T0.3 |
| Entregas | `src/aml_guardian/contracts/runtime.py`: DT-12 com os campos do RF-11 + `seq`, `event_key`, `occurred_at`; DT-13; DT-14 com registro `schema_version → modelo` e validação de `payload`; DT-15 com `justification` ≥ 50 caracteres; `InvestigationState` com os campos do `SPEC.md` §9.3 |
| Teste focado | `tests/contracts/test_dt_envelope.py` · `pytest -k dt_envelope`: envelope com `payload` divergente do `schema_version` rejeitado; `type` fora de {`TASK`, `RESULT`, `ERROR`} rejeitado; DT-15 com 49 caracteres rejeitado; `InvestigationState` serializa e desserializa sem perda |
| Ask First | Qualquer mudança no envelope ou no estado do grafo (`SOUL.md` §4) |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde; `pytest tests/contracts` verde (contratos DT-01 a DT-16 completos) |

## T0.5 — Configuração versionada

| Campo | Conteúdo |
| :--- | :--- |
| IDs | RF-03, RF-04, RF-06, RF-09, RNF-09, RNF-10, ADR-010, ADR-013 |
| Depende de | T0.1 |
| Entregas | `config/triage_rules.yaml` (`rules_version: 1`, os quatro detectores críticos do RF-03 com parâmetros); `config/litellm.yaml` (alias do modelo → Ollama `qwen2.5:1.5b`, `temperature 0`, `seed 42`, `num_ctx 2048`, `num_predict 60`, `num_thread 8`; `cache.threshold` 0,92; `selection.min_score`); `config/baseline.yaml` (`tempo_manual_min`, `tempo_revisao_min`); `config/feriados.yaml` (feriados nacionais do período dos dados sintéticos, com a fonte oficial citada no arquivo); `src/aml_guardian/config/` com loaders Pydantic |
| Teste focado | `tests/config/test_config.py` · `pytest -k config`: arquivo sem `rules_version` falha; parâmetro de tipo errado falha; `litellm.yaml` sem nome de provedor proprietário em código |
| Ask First | Valores provisórios dos limiares da `rules_version` 1; valores de `baseline.yaml` (premissas do autor, `SPEC.md` §12); valor provisório de `selection.min_score` |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde e cada valor aprovado registrado com data no próprio arquivo |

## T0.6 — Mapeamento de tipologias e loader do SAML-D

| Campo | Conteúdo |
| :--- | :--- |
| IDs | `SPEC.md` §3.2, §8.3 |
| Depende de | T0.3 |
| Entregas | `data/mappings/saml_d_to_cc4001.yaml` com `mapping_version: 1` e, por tipologia, incisos e `critica` exatamente como a tabela do `SPEC.md` §3.2 (`article_ref` preferenciais e `applicability` ficam vazios até T1.8/M4); `src/aml_guardian/sourcedata/saml_loader.py` que valida colunas e rótulos contra o mapeamento |
| Teste focado | `tests/sourcedata/test_saml_loader.py` · `pytest -k saml_loader`: CSV sintético gerado no teste com coluna renomeada falha citando a coluna; rótulo de tipologia ausente do mapeamento falha citando o rótulo; mapeamento com as 6 críticas do `SPEC.md` §3.2 |
| Ask First | Qualquer divergência encontrada no SAML-D real em relação ao mapeamento |
| Pré-requisito | Autor coloca o CSV do SAML-D em `data/raw/` (licença CC BY-NC-SA 4.0, não versionado) |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde e loader executado com sucesso sobre o arquivo real |

## T0.7 — Camada brasileira e `core_sintetico.sqlite`

| Campo | Conteúdo |
| :--- | :--- |
| IDs | DT-02, DT-03, `SPEC.md` §8.1, §8.3 |
| Depende de | T0.6 |
| Entregas | `src/aml_guardian/sourcedata/brazil_layer.py`: conversão para BRL por taxa fixa sintética configurada; `Payment_type` do SAML-D → enum do DT-02; clientes DT-03 com CPF/CNPJ de DV válido gerados por seed; listas de restrição sintéticas (`pep`, `ceis`, `cnep`); gravação em `data/core_sintetico.sqlite` |
| Teste focado | `tests/sourcedata/test_camada_br.py` · `pytest -k camada_br`: todo CPF/CNPJ gerado tem DV válido; mesma seed → mesmo conteúdo (hash); todo `Payment_type` do SAML-D mapeado; nenhum CPF/CNPJ literal versionado |
| Ask First | Taxa de câmbio sintética e mapeamento `Payment_type` → DT-02, se não forem diretos |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde e banco gerado a partir do SAML-D real |

## T0.8 — Gerador de alertas legado simulado

| Campo | Conteúdo |
| :--- | :--- |
| IDs | DT-01, `SPEC.md` §3.1 (Circ. 3.978/2020, art. 39, parágrafo único), §8.3 |
| Depende de | T0.7 |
| Entregas | `src/aml_guardian/sourcedata/alert_generator.py`: regras de limiar e contagem em janela móvel por conta remetente sobre `core_sintetico`; `selected_at` e `occurrence_window`; relatório da taxa de falso positivo |
| Teste focado | `tests/sourcedata/test_gerador_alertas.py` · `pytest -k gerador_alertas`: 0 alertas com transação ocorrida mais de 45 dias antes de `selected_at`; todo alerta valida no DT-01; taxa de falso positivo calculada e reportada |
| Ask First | Regras e limiares do monitoramento legado simulado |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde e taxa de falso positivo registrada no `MEMORY.md` |

## T0.9 — Conjunto de desenvolvimento e golden set `v1`

| Campo | Conteúdo |
| :--- | :--- |
| IDs | `SPEC.md` §8.3 |
| Depende de | T0.8 |
| Entregas | `src/aml_guardian/sourcedata/golden.py`: separação disjunta entre conjunto de desenvolvimento e `data/golden/v1` com 500 alertas (30 por tipologia crítica = 180; 10 por suspeita não crítica = 110; 210 normais cobrindo as 11 tipologias normais); manifesto com seed, `alert_id`s, estrato e SHA-256; novo ADR-014 em `docs/ADR.md` com o formato do golden set |
| Teste focado | `tests/sourcedata/test_golden.py` · `pytest -k golden`: contagem por estrato; regeneração reproduz o hash do manifesto; interseção de `alert_id` e de transações entre desenvolvimento e golden = ∅ |
| Ask First | **ADR-014:** formato do golden set — manifesto versionado; payloads (DT-01, com dado pessoal sintético) regenerados deterministicamente e fora do git |
| Revisores | Revisor Python |
| Pronto quando | Filtro verde, hash gravado no manifesto e ADR-014 aprovado |

## Gate de saída do M0

| Item do `PLAN.md` §3 | Tarefa | Evidência |
| :--- | :--- | :--- |
| Testes de contrato de todos os DT verdes | T0.2, T0.3, T0.4 | `pytest tests/contracts` |
| Loader com coluna divergente falha com mensagem explícita | T0.6 | `pytest -k saml_loader` |
| Nenhum alerta com ocorrência > 45 dias | T0.8 | `pytest -k gerador_alertas` |
| Hash do golden set gravado e disjunção verificada | T0.9 | `pytest -k golden` |
| Nenhum dado real de cliente ou lista de restrição real | T0.7, T0.8, T0.9 | `pytest -k "camada_br or golden"` + revisão do diff |
| Revisor Python | Todas | Achados altos resolvidos |

