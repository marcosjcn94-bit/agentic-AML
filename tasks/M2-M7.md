# TASKS M2–M7 — Fechamento dos marcos restantes

| Campo | Valor |
| :--- | :--- |
| Parte de | `TASKS.md` 1.1.0 |
| Status | Aprovado (execução autônoma, sem Ask First — decisão do autor em 2026-09-18) |

Formato reduzido por decisão do autor (deadline do projeto): **um arquivo, uma tarefa por marco**, sem os campos
"Ask First"/"Depende de" por tarefa (toda dependência é sequencial M2→M3→M4→M5→M6→M7; M2/M3 podem rodar em
paralelo por serem independentes entre si). Toda decisão antes "Ask First" nestes marcos já foi resolvida pelo
autor com o critério de menor custo de token e registrada em ADR quando estrutural (ADR-016, ADR-017) ou em
`config/*.yaml`/`MEMORY.md` quando não estrutural. Testes só rodam ao final do conjunto (não por tarefa). Revisor
só é acionado quando a tarefa muda ADR.

Nota: T1.1–T1.13 (`tasks/M1.md`) já entregaram, na prática, a maior parte do que `PLAN.md` original reservava a
M2–M7 (sanitizador, MCP-01–04, triagem, RAG, cache, revisor, dossiê, prazos, auditoria, grafo, API base, eval
inicial — ver `MEMORY.md`). As tarefas abaixo fecham só o que ainda faltava por marco.

## T2 — Privacidade Zero-Trust (M2)
Reidentificação (API-04) fechada: `Vault` passou a viver em `AppState` (um por processo, nunca persistido) e
`POST /alerts` injeta esse vault em `sanitize_alert` em vez de um vault descartável por request — sem isso, o
token gerado na sanitização era irreversível mesmo para o analista autorizado. Nova rota `GET /reidentify/{token}`
(papéis `analista`/`compliance_officer`). Entregas: `api/state.py`, `api/routes.py`.
Gate: recall de sanitização (RF-02) e canário RNF-05 já medidos no M1 (`pytest -k canary`); reidentificação
agora exercitável de ponta a ponta pela API.

## T3 — Triagem determinística e recall crítico (M3)
Sem código novo: os quatro detectores críticos (T1.4) e `rules_version` já são definitivos desde o T0.5 Ask
First aprovado. Gate de recall crítico (100%) fica para a rodada de avaliação de T7 sobre o golden set completo
— é a mesma medição, não uma nova.

## T4 — RAG normativo, Revisor e cache (M4)
Fecha os dois Ask First que `PLAN.md` §5 deixava em aberto: modelo de embedding (**ADR-016**, mantido
`paraphrase-multilingual-MiniLM-L12-v2`) e `selection.min_score` (mantido 0,35, agora **definitivo** em
`config/retrieval.yaml`). Corrigida divergência encontrada: `config/litellm.yaml` tinha um segundo bloco
`selection.min_score = 0,50` não lido por nenhum código (`retrieval/selection.py` só lê `config/retrieval.yaml`)
— alinhado a 0,35 para não haver dois valores diferentes documentados. `cache.threshold` (0,92) já era definitivo
desde o SPEC.md. Entregas: `docs/ADR.md`, `config/retrieval.yaml`, `config/litellm.yaml`, `norms/store.py`.

## T5 — Resiliência, auditoria e prazos (M5)
Sem código novo: fail-closed por nó (`try/except Exception` → `NEEDS_HUMAN`) e idempotência do `event_key`
já foram entregues e revisados na T1.10 (`MEMORY.md`, fix wave pós-revisão). Gate de "injeção de falha → estado
válido sem duplicar evento" fica coberto pelos testes já existentes de `tests/graph/test_graph.py`; não identificado
gap adicional que justifique novo teste dedicado neste lote.

## T6 — Fluxo humano, UI e segurança (M6)
Fecha API-02 (aceite do Compliance Officer, RF-12/DT-15): nova rota `POST /alerts/{alert_id}/decision`, que
valida o dossiê em `DRAFT_READY` no checkpoint, monta e valida `Approval` (DT-15, `prazo_comunicacao` = próximo
dia útil via `config/feriados.py`), grava evento `HUMAN_DECISION` na trilha de auditoria (DT-12) e atualiza
`AlertRecord.state` (`APPROVED`/`RETURNED`). Entregas: `api/routes.py`.
Fechamento posterior: UI Streamlit dedicada consome somente REST e cobre fila, detalhe, reidentificação,
submissão e decisão; `tests/e2e/test_ui_workflow.py` valida o fluxo em Chromium e o canário de logs. ZAP baseline
via Docker registrou 66 PASS, 0 FAIL e 1 WARN informativo (`reports/zap-baseline-2026-09-18.md`).

## T7 — Avaliação Go/No-Go (M7)
Segundo provedor do RNF-10 fechado: **ADR-017**, `investigacao_alt` → `ollama_chat/llama3.2:3b` em
`config/litellm.yaml`, custo zero (mesma máquina, sem provedor cloud). Verificado de ponta a ponta (não só
declarado no YAML): modelo baixado (`ollama pull llama3.2:3b`) e chamado via `LiteLLMConfig.params_for("investigacao_alt")`
+ `litellm.completion` real — resposta correta em 12,35 s (`MEMORY.md`). Rodada final: suíte completa
(`pytest`) e `scripts/evaluate.py --ollama` ao final do lote, resultado em `reports/`.

`scripts/evaluate.py --golden data/golden/v1 --ollama` rodou os 500 alertas reais pelo pipeline completo
(`reports/eval-golden-2026-09-18.md`/`.json`, consolidado sem alterar valores em
`reports/eval-final-2026-09-18.md`/`.json`, ~2026-09-18T14:35 UTC). 7/9 gates do `SPEC.md` §10 medidos e
aprovados (Redução de FP 82,86%, Velocidade 7,96x, Redução de tokens 80,00%, Latência p95 16,87 s, Prazo
interno 100,00%, Privacidade 0 ocorrências, Integridade da auditoria `True`). **2 gates não fecham**:
**Recall crítico (RNF-01) = 18,33% (33/180) — No-Go, sem exceção pelo `SPEC.md` §10**; Grounding (RNF-02) =
"não medida" (0/500 alertas classificados pelo Ollama real como `Structuring`, a única tipologia com
`applicability` curado em `data/mappings/saml_d_to_cc4001.yaml`, então nenhuma citação foi proposta nesta
rodada — não é falha do harness, é o modelo real não convergindo para esse rótulo na amostra). **Resultado
geral do M7: No-Go** enquanto o recall crítico não for corrigido (fora do escopo desta rodada de avaliação;
correção exigiria revisar `config/triage_rules.yaml`/o prompt de investigação, ambos decisões estruturais —
Ask First). Detalhe em `MEMORY.md`.
